# تعليمات تشغيل نظام المخزون

## تشغيل النظام

اضغط مرتين على `start.bat`. انتظر حتى تفتح صفحة النظام في المتصفح.

## إيقاف النظام

اضغط مرتين على `stop.bat` بعد انتهاء العمل. لا يحذف هذا الإجراء البيانات.

## إعادة التشغيل

إذا لم يفتح النظام بشكل صحيح، اضغط مرتين على `restart.bat` ثم انتظر.

## نسخة احتياطية

اضغط مرتين على `backup.bat`. ستظهر رسالة بمكان النسخة. توجد النسخ في مجلد `backups` بجانب النظام. انسخ هذا المجلد بانتظام إلى USB أو مكان آمن خارج الكمبيوتر.

## إذا لم يفتح النظام

تأكد من تشغيل Docker Desktop، ثم شغّل `start.bat` مرة أخرى. إذا استمرت المشكلة، شغّل `logs.bat` وأرسل النتيجة للمطور.

## مهم جداً

لا تحذف مجلد `backups` أو ملف `.env.docker` أو Docker Desktop. لا تستخدم ملفات أخرى لحذف البيانات. اتصل بالمطور قبل الاستعادة أو التحديث أو نقل النظام إلى جهاز آخر.
