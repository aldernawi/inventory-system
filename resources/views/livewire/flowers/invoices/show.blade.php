<section class="space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row"><div><p class="text-sm font-bold text-rose-800">فاتورة ورد</p><h1 class="mt-1 font-mono text-2xl font-bold">{{ $invoice->invoice_number }}</h1><p class="mt-1 text-sm text-slate-600">{{ $invoice->recipient_name ?? 'بدون مستلم' }} · {{ $invoice->invoice_date->format('Y-m-d') }}</p></div><div class="flex flex-wrap gap-2"><a href="{{ route('flowers.invoices.print',$invoice) }}" target="_blank" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-bold">طباعة</a>@if($isDraft)<a wire:navigate href="{{ route('flowers.invoices.edit',$invoice) }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-bold">تعديل المسودة</a><button wire:click="confirm" class="rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white">اعتماد وخصم المخزون</button><button wire:click="deleteDraft" wire:confirm="هل تريد حذف المسودة؟" class="px-4 py-2.5 text-sm font-bold text-rose-700">حذف المسودة</button>@endif</div></div>@error('invoice')<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{{ $message }}</div>@enderror
<div class="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2 lg:grid-cols-4"><div><p class="text-xs font-bold text-slate-500">الحالة</p><p class="mt-1 font-bold">{{ $isDraft?'مسودة':($isConfirmed?'معتمدة':'ملغاة') }}</p></div><div><p class="text-xs font-bold text-slate-500">السداد</p><p class="mt-1 font-bold">{{ $invoice->payment_status->value }}</p></div><div><p class="text-xs font-bold text-slate-500">أنشأها</p><p class="mt-1 font-bold">{{ $invoice->createdBy?->name }}</p></div><div><p class="text-xs font-bold text-slate-500">اعتمدها</p><p class="mt-1 font-bold">{{ $invoice->confirmedBy?->name ?? '—' }}</p></div><div class="sm:col-span-2 lg:col-span-4"><p class="text-xs font-bold text-slate-500">ملاحظات</p><p class="mt-1 whitespace-pre-line text-sm">{{ $invoice->notes ?? '—' }}</p></div></div>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-right text-sm"><thead class="bg-slate-50 text-xs font-bold text-slate-500"><tr><th class="px-4 py-3">الورد</th><th class="px-4 py-3">اللون</th><th class="px-4 py-3">الوحدة</th><th class="px-4 py-3">الكمية</th><th class="px-4 py-3">السعر</th><th class="px-4 py-3">الإجمالي</th><th class="px-4 py-3">رصيد البيع</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($invoice->items as $item)<tr><td class="px-4 py-3 font-bold">{{ $item->product_name }}</td><td class="px-4 py-3">{{ $item->color ?? '—' }}</td><td class="px-4 py-3">{{ $item->unit }}</td><td class="px-4 py-3">{{ $item->quantity }}</td><td class="px-4 py-3">{{ $item->unit_price }}</td><td class="px-4 py-3 font-bold">{{ $item->line_total }}</td><td class="px-4 py-3">{{ $item->stockMovement ? $item->stockMovement->balance_before.' ← '.$item->stockMovement->balance_after : '—' }}</td></tr>@endforeach</tbody></table></div><div class="grid gap-3 border-t bg-slate-50 p-4 text-sm sm:grid-cols-2 lg:grid-cols-5">@foreach(['subtotal_amount'=>'المجموع الفرعي','discount_amount'=>'الخصم','total_amount'=>'الإجمالي','paid_amount'=>'المدفوع','remaining_amount'=>'المتبقي'] as $key=>$label)<div><span class="text-slate-500">{{ $label }}</span><p class="mt-1 text-lg font-bold">{{ $invoice->{$key} }}</p></div>@endforeach</div></div>
@if($invoice->status->value==='cancelled')<div class="rounded-2xl border border-rose-200 bg-rose-50 p-5"><h2 class="font-bold">بيانات الإلغاء</h2><p class="mt-2 text-sm">بواسطة {{ $invoice->cancelledBy?->name }} · {{ $invoice->cancelled_at?->format('Y-m-d H:i') }}</p><p class="mt-2 whitespace-pre-line text-sm">{{ $invoice->cancellation_reason }}</p></div>@endif @can('cancel-flower-invoices')@if($isConfirmed)<div class="rounded-2xl border border-rose-200 bg-white p-5"><h2 class="font-bold text-rose-900">إلغاء الفاتورة</h2><p class="mt-1 text-sm text-slate-600">سيتم إنشاء حركات عكس لاستعادة المخزون، ولا يمكن التراجع عن الإلغاء.</p><textarea wire:model="cancellationReason" rows="2" class="mt-3 w-full rounded-lg border-slate-300" placeholder="سبب الإلغاء"></textarea>@error('cancellation_reason')<p class="mt-1 text-xs text-rose-600">{{ $message }}</p>@enderror<button wire:click="cancel" wire:confirm="هل تريد إلغاء الفاتورة وعكس حركات البيع؟" class="mt-3 rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white">إلغاء وعكس المخزون</button></div>@endif@endcan</section>
