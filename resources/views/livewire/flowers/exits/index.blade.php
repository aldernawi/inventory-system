<section class="space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold text-rose-800">خروج غير بيعي</p><h1 class="mt-1 text-2xl font-bold text-slate-950">سجل خروج الورد</h1><p class="mt-1 text-sm text-slate-600">لا يتضمن هذا السجل مبيعات أو عملاء.</p></div>@can('operate-flower-exits')<a wire:navigate href="{{ route('flowers.exits.create') }}" class="rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white">تسجيل خروج</a>@endcan</div><div class="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-2 lg:grid-cols-4"><select wire:model.live="productId" class="rounded-lg border-slate-300 text-sm"><option value="">كل الأنواع</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select><select wire:model.live="exitType" class="rounded-lg border-slate-300 text-sm"><option value="">كل الأنواع</option>@foreach($exitTypes as $type)<option value="{{ $type->value }}">{{ $this->exitTypeLabel($type) }}</option>@endforeach</select><input wire:model.live="from" type="date" class="rounded-lg border-slate-300 text-sm"><input wire:model.live="to" type="date" class="rounded-lg border-slate-300 text-sm"></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-right"><thead class="bg-slate-50 text-xs font-bold text-slate-500"><tr><th class="px-4 py-3">التاريخ</th><th class="px-4 py-3">نوع الورد</th><th class="px-4 py-3">الكمية</th><th class="px-4 py-3">النوع</th><th class="px-4 py-3">المستلم</th><th class="px-4 py-3">بواسطة</th><th class="px-4 py-3">ملاحظات</th></tr></thead><tbody class="divide-y divide-slate-100 text-sm">@forelse($exits as $exit)<tr><td class="px-4 py-3">{{ $exit->exit_date->format('Y-m-d') }}</td><td class="px-4 py-3 font-bold">{{ $exit->product->name }}</td><td class="px-4 py-3 font-bold text-rose-700">{{ $exit->quantity }}</td><td class="px-4 py-3">{{ $this->exitTypeLabel($exit->exit_type) }}</td><td class="px-4 py-3">{{ $exit->recipient_name ?? '—' }}</td><td class="px-4 py-3">{{ $exit->createdBy->name }}</td><td class="px-4 py-3 text-slate-600">{{ $exit->notes ?? '—' }}</td></tr>@empty<tr><td colspan="7" class="px-4 py-10 text-center text-sm text-slate-500">لا توجد عمليات خروج مطابقة.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-4 py-3">{{ $exits->links() }}</div></div></section>
