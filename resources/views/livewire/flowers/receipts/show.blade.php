<section class="space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold text-rose-800">إيصال استلام ورد</p><h1 class="mt-1 font-mono text-2xl font-bold text-slate-950">{{ $receipt->receipt_number }}</h1><p class="mt-1 text-sm text-slate-600">{{ $receipt->supplier->name }} · {{ $receipt->receipt_date->format('Y-m-d') }}</p></div><div class="flex flex-wrap gap-2">
@if($isDraft)
@can('operate-flower-receipts')
<a wire:navigate href="{{ route('flowers.receipts.edit', $receipt) }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-bold text-slate-700">تعديل المسودة</a><button wire:click="confirm" wire:loading.attr="disabled" class="rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white">اعتماد</button><button wire:click="deleteDraft" wire:confirm="هل تريد حذف هذه المسودة؟" class="rounded-lg px-4 py-2.5 text-sm font-bold text-rose-700">حذف المسودة</button>
@endcan
@endif
</div></div>@error('receipt')<div class="rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{{ $message }}</div>@enderror<div class="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2 lg:grid-cols-4"><div><p class="text-xs font-bold text-slate-500">الحالة</p><p class="mt-1 font-bold">{{ $isDraft ? 'مسودة' : 'معتمد' }}</p></div><div><p class="text-xs font-bold text-slate-500">فاتورة المورد</p><p class="mt-1 font-bold">{{ $receipt->supplier_invoice_number ?? '—' }}</p></div><div><p class="text-xs font-bold text-slate-500">أنشأه</p><p class="mt-1 font-bold">{{ $receipt->createdBy->name }}</p></div><div><p class="text-xs font-bold text-slate-500">اعتمده</p><p class="mt-1 font-bold">{{ $receipt->confirmedBy?->name ?? '—' }}</p></div><div class="sm:col-span-2 lg:col-span-4"><p class="text-xs font-bold text-slate-500">ملاحظات</p><p class="mt-1 whitespace-pre-line text-sm text-slate-700">{{ $receipt->notes ?? '—' }}</p></div></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-right"><thead class="bg-slate-50 text-xs font-bold text-slate-500"><tr><th class="px-4 py-3">نوع الورد / اللون</th><th class="px-4 py-3">الوحدة</th><th class="px-4 py-3">المتوقع</th><th class="px-4 py-3">الواصل</th><th class="px-4 py-3">النقص</th><th class="px-4 py-3">الزيادة</th><th class="px-4 py-3">تالف الوصول</th><th class="px-4 py-3">المضاف</th><th class="px-4 py-3">قبل ← بعد</th><th class="px-4 py-3">سعر الشراء</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($receipt->items as $item)<tr><td class="px-4 py-3 font-bold">@if($item->product)<a wire:navigate href="{{ route('flowers.products.show', $item->product) }}" class="hover:text-rose-700">{{ $item->product_name }}</a>@else{{ $item->product_name }}@endif<p class="text-xs font-normal text-slate-500">{{ $item->color ?? '—' }}</p></td><td class="px-4 py-3">{{ $item->unit }}</td><td class="px-4 py-3">{{ $item->expected_quantity }}</td><td class="px-4 py-3">{{ $item->received_quantity }}</td><td class="px-4 py-3 text-rose-700">{{ $item->shortage_quantity }}</td><td class="px-4 py-3 text-emerald-700">{{ $item->surplus_quantity }}</td><td class="px-4 py-3 text-rose-700">{{ $item->damaged_quantity }}</td><td class="px-4 py-3 font-bold text-rose-900">{{ $item->accepted_quantity }}</td><td class="px-4 py-3 font-bold">{{ $item->balance_before ?? '—' }} <span class="text-slate-400">←</span> {{ $item->balance_after ?? '—' }}</td><td class="px-4 py-3">{{ $item->purchase_price ?? '—' }}</td></tr>@endforeach</tbody></table></div></div></section>
