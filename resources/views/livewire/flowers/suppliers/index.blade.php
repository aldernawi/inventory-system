<section class="space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold text-rose-800">بيانات أساسية</p><h1 class="mt-1 text-2xl font-bold text-slate-950">موردو الورد</h1><p class="mt-1 text-sm text-slate-600">تظهر هنا فقط جهات الموردين المخصصة للورد أو لكلا النظامين.</p></div>@can('manage-flower-master-data')<a wire:navigate href="{{ route('flowers.suppliers.create') }}" class="rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white">إضافة مورد</a>@endcan</div><div class="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><input wire:model.live.debounce.300ms="search" type="search" placeholder="ابحث بالاسم أو الشركة أو الهاتف" class="w-full rounded-lg border-slate-300 text-sm"></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-right"><thead class="bg-slate-50 text-xs font-bold text-slate-500"><tr><th class="px-4 py-3">المورد</th><th class="px-4 py-3">الشركة</th><th class="px-4 py-3">الهاتف</th><th class="px-4 py-3">النطاق</th><th class="px-4 py-3">الحالة</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100 text-sm">@forelse($suppliers as $supplier)<tr><td class="px-4 py-3 font-bold">{{ $supplier->name }}</td><td class="px-4 py-3 text-slate-600">{{ $supplier->company_name ?? '—' }}</td><td class="px-4 py-3 text-slate-600">{{ $supplier->phone ?? '—' }}</td><td class="px-4 py-3">{{ $supplier->module_scope->value === 'both' ? 'الورد والسلامي' : 'الورد فقط' }}</td><td class="px-4 py-3">{{ $supplier->is_active ? 'نشط' : 'غير نشط' }}</td><td class="px-4 py-3">@can('manage-flower-master-data')<div class="flex gap-3 text-xs font-bold"><a wire:navigate href="{{ route('flowers.suppliers.edit', $supplier) }}" class="text-rose-800">تعديل</a><button wire:click="toggleActive({{ $supplier->id }})" wire:confirm="هل تريد تغيير حالة المورد؟" class="text-slate-600">{{ $supplier->is_active ? 'إيقاف' : 'تفعيل' }}</button></div>@endcan</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-sm text-slate-500">لا يوجد موردون للورد.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-4 py-3">{{ $suppliers->links() }}</div></div></section>
