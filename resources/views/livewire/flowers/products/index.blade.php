<section class="space-y-6">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold text-rose-800">بيانات أساسية</p><h1 class="mt-1 text-2xl font-bold text-slate-950">أنواع الورد</h1><p class="mt-1 text-sm text-slate-600">الرصيد الحالي للعرض فقط؛ كل تغيير له حركة مخزون مدققة.</p></div>@can('manage-flower-master-data')<a wire:navigate href="{{ route('flowers.products.create') }}" class="rounded-lg bg-rose-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-rose-700">إضافة نوع ورد</a>@endcan</div>
    <div class="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div class="grid gap-3 sm:grid-cols-[1fr_11rem]"><input wire:model.live.debounce.300ms="search" type="search" placeholder="ابحث بالاسم أو الكود أو اللون" class="rounded-lg border-slate-300 text-sm"><select wire:model.live="status" class="rounded-lg border-slate-300 text-sm"><option value="all">كل الحالات</option><option value="active">نشط</option><option value="inactive">غير نشط</option></select></div></div>
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-right"><thead class="bg-slate-50 text-xs font-bold text-slate-500"><tr><th class="px-4 py-3">نوع الورد</th><th class="px-4 py-3">الكود</th><th class="px-4 py-3">اللون / الدرجة</th><th class="px-4 py-3">الوحدة</th><th class="px-4 py-3">الرصيد</th><th class="px-4 py-3">الحالة</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100 text-sm">@forelse($products as $product)<tr class="hover:bg-slate-50"><td class="px-4 py-3 font-bold"><a wire:navigate href="{{ route('flowers.products.show', $product) }}" class="hover:text-rose-700">{{ $product->name }}</a></td><td class="px-4 py-3 font-mono text-slate-600">{{ $product->code }}</td><td class="px-4 py-3 text-slate-600">{{ collect([$product->color, $product->grade])->filter()->join(' · ') ?: '—' }}</td><td class="px-4 py-3 text-slate-600">{{ $product->unit }}</td><td class="px-4 py-3 font-bold">{{ $product->current_quantity }}</td><td class="px-4 py-3"><span @class(['rounded-full px-2.5 py-1 text-xs font-bold','bg-emerald-100 text-emerald-800'=>$product->is_active,'bg-slate-200 text-slate-700'=>!$product->is_active])>{{ $product->is_active ? 'نشط' : 'غير نشط' }}</span></td><td class="px-4 py-3">@can('manage-flower-master-data')<div class="flex gap-3 text-xs font-bold"><a wire:navigate href="{{ route('flowers.products.edit', $product) }}" class="text-rose-800">تعديل</a><button wire:click="toggleActive({{ $product->id }})" wire:confirm="هل تريد تغيير حالة هذا النوع؟" class="text-slate-600">{{ $product->is_active ? 'إيقاف' : 'تفعيل' }}</button></div>@endcan</td></tr>@empty<tr><td colspan="7" class="px-4 py-10 text-center text-sm text-slate-500">لا توجد أنواع ورد مطابقة.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-100 px-4 py-3">{{ $products->links() }}</div></div>
</section>
